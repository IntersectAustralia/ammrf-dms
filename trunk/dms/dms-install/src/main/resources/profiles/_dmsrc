# SETUP_TYPE : single | server | worker
SETUP_TYPE=single

# base dir to install all software, e.g. /usr/local, /usr/share, /opt or whatever makes sense in the system
INSTALL_DIR=/home/carlos/Programs

# the user account to use
DMS_USER=carlos

#Certificate and keystore properties
KEYSTORE_PASS=dms-password
CERTIFICATE_ALIAS=dms
